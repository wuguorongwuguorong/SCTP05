const prompt = require("prompt-sync")();

let length =Number(prompt("Enter the length of a rectangle:"));
let width = Number(prompt("Enter the witdth here:"));

const calPerimeter= 2*(length + breathe)
console.log(calPerimeter);
